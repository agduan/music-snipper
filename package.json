{
  "name": "music-snippet-saver",
  "version": "1.0.0",
  "description": "Personal web app for saving and replaying favorite moments in songs",
  "scripts": {
    "dev": "cd frontend && vite",
    "build": "cd frontend && vite build",
    "deploy": "npm run build && firebase deploy"
  },
  "dependencies": {
    "firebase": "^11.10.0",
    "framer-motion": "^12.34.3",
    "lucide-react": "^0.468.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.5"
  }
}
